apiVersion: v1
kind: Secret
metadata:
  name: gardener-controller-secret
  namespace: default
  annotations:
    bootstrap.kcp.io/create-only: "true"
    kubernetes.io/service-account.name: gardener-controller
type: kubernetes.io/service-account-token